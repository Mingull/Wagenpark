export type Vehicle = {
    id: string;
    color: string;
    type: string;
    model: string;
    manufacturer: string;
    vehicle: string;
    createdAt: string;
}

export type User = {
    id: string;
    username: string;
}